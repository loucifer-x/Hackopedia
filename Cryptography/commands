https://quipqiup.com/


gpg privacy guard
gpg --output [OUTPUT.txt] --decrypt [file.gpg]
--armor 
--symmetric

OpenSSL
  openssl [CIPHER] -in [INPUT.TXT] -out [OUTPUT.TXT]
  -e | encrypt
  -d | decrypt
  -iter [number] | 
  -pbkdf2 | password based encyption
  
  Ciphers - aes-256-cbc, AES-256-GCM, ChaCha20-Poly1305

  penssl pkeyutl -decrypt -in [CIPHER.TXT] -inkey [PRIVATE KEY.pem] -out [OUTPUT.TXT]
  openssl rsa -in [PRIVATE KEY].pem -text -noout
  openssl dhparam -out dhparams.pem 2048



strings [file]

binwalk -e [FILE]

steghide extract -sf image.jpg

stegseek image.jpg /usr/share/wordlists/rockyou.txt
