**Commands**

SHOW TABLES;


**Notes**

Anything after -- on the same line is ignored by the database.



**Filter Evasion Techniques**

URL Encoding - Common encoding method where characters are represetned using **%** followed by their ASCII value.
```' OR 1=1--``` -> ```%27%20OR%201%3D1--```

Hexadecimal Encoding:

Unicode Encoding

